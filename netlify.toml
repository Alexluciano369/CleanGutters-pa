[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# This domain is for paid campaign landing pages. Organic search belongs to cleangutterslighting.com.
[[headers]]
  for = "/*"
  [headers.values]
    X-Robots-Tag = "noindex, follow"
